function setup() {
    createCanvas(windowWidth, windowHeight - 150);

    h1 = createElement('h1', 'something new');
    h1.position(windowWidth / 2, 50);

    h1 = createElement('h1', 'my js projects');
    h1.position(windowWidth / 2, 500);
}